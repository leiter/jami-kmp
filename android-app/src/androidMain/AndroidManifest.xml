<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Contacts sync -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- Foreground service type permissions (Android 14+ enforcement) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />

    <!-- Auto-start after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Telecom API: self-managed ConnectionService for call log + Bluetooth routing -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <application
        android:name=".JamiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.JamiKmp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:enableOnBackInvokedCallback="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustNothing"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden"
            android:theme="@style/Theme.JamiKmp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="net.jami.action.VIEW_CALL" />
                <action android:name="net.jami.action.ACCEPT_CALL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- Share-to-Jami: receive content from the Android share sheet -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- URI scheme deep links: ring://, jami://, sip:, tel: -->
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ring" />
                <data android:scheme="jami" />
                <data android:scheme="sip" />
                <data android:scheme="tel" />
            </intent-filter>
        </activity>

        <!-- FileProvider for sharing camera captures and other files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Background sync service -->
        <service
            android:name="net.jami.services.JamiSyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Persistent daemon service: keeps the process alive for incoming calls and messages -->
        <service
            android:name=".service.JamiDaemonService"
            android:exported="false"
            android:foregroundServiceType="dataSync|remoteMessaging"
            android:stopWithTask="false" />

        <!-- Boot receiver: starts JamiDaemonService after device reboot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.REBOOT" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Call foreground service: keeps microphone/audio alive while app is backgrounded.
             mediaProjection is intentionally omitted — it requires an active capture session
             at startForeground() time (API 34+), which a regular call does not have.
             Screen sharing acquires its own projection token on demand via MainActivity. -->
        <service
            android:name=".service.CallNotificationService"
            android:exported="false"
            android:foregroundServiceType="microphone|phoneCall|camera" />

        <!-- Broadcast receiver for notification Answer / Decline / Hang Up actions -->
        <receiver
            android:name=".service.CallActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="net.jami.action.ANSWER_CALL" />
                <action android:name="net.jami.action.DECLINE_CALL" />
                <action android:name="net.jami.action.HANGUP_CALL" />
            </intent-filter>
        </receiver>

        <!-- Broadcast receiver for message notification Reply / Mark Read actions -->
        <receiver
            android:name=".service.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="net.jami.action.REPLY_MESSAGE" />
                <action android:name="net.jami.action.MARK_READ" />
            </intent-filter>
        </receiver>

        <!-- Telecom ConnectionService: bridges Android Telecom framework with Jami calls.
             The BIND_TELECOM_CONNECTION_SERVICE permission ensures only the system can bind. -->
        <service
            android:name=".service.JamiConnectionService"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>

    </application>

</manifest>
